import { connect } from 'react-redux';
import { compose } from 'redux';
import InTheLesson from './IntheLesson';


let mapStateToProps = (state) => {
    return {
        
        
    }
}

export default compose (
    connect(mapStateToProps, {})
)
(InTheLesson);